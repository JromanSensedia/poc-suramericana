apiVersion: v1
kind: Service
metadata:
    name: jromansensediapocsuramericana
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: jromansensediapocsuramericana